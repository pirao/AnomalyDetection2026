# Local Model Cache Placeholder

This directory is used for local fitted-model artifacts generated by the private benchmark workflow.

Expected local layout after running the full private workflow:

```text
models/
  meta.json
  v1/
    meta.json
    1.pkl
    ...
```

The `models/` directory and `.pkl` files are ignored by Git and should not be committed.
